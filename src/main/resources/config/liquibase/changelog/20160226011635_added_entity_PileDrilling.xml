<?xml version="1.0" encoding="utf-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.4.xsd">

    <property name="now" value="now()" dbms="mysql,h2"/>
    <property name="now" value="current_timestamp" dbms="postgresql"/>
    <property name="now" value="sysdate" dbms="oracle"/>

    <property name="autoIncrement" value="true" dbms="mysql,h2,postgresql,oracle"/>

    <property name="floatType" value="float4" dbms="postgresql, h2"/>
    <property name="floatType" value="float" dbms="mysql, oracle"/>

    <!--
        Added the entity PileDrilling.
    -->
    <changeSet id="20160226011635" author="jhipster">
        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="pile_drilling"/>
            </not>
        </preConditions>
        <createTable tableName="pile_drilling">
            <column name="id" type="bigint" autoIncrement="${autoIncrement}">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="drilling_machine" type="varchar(255)"/>

            <column name="project_depth" type="decimal(10,2)"/>

            <column name="effective_depth" type="decimal(10,2)"/>

            <column name="start_date" type="date"/>

            <column name="end_date" type="date"/>

            <column name="start_time" type="date"/>

            <column name="end_time" type="date"/>

            <column name="drilling_id" type="bigint"/>

            <column name="user_id" type="bigint"/>
            <!-- jhipster-needle-liquibase-add-column - Jhipster will add columns here, do not remove-->
        </createTable>

        <addForeignKeyConstraint baseColumnNames="user_id"
                                 baseTableName="pile_drilling"
                                 constraintName="fk_piledrilling_user_id"
                                 referencedColumnNames="id"
                                 referencedTableName="jhi_user"/>

    </changeSet>
</databaseChangeLog>
